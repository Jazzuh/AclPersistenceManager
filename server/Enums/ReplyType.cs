namespace AclManager.Server.Enums
{
    /// <summary>
    /// Enum represents the type of reply message that will be sent back to the client
    /// </summary>
    internal enum ReplyType
    {
        Error,
        Warning,
        Success
    }
}
